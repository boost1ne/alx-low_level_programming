#include "main.h"
#include <stdio.h>

/**
 * main - Prints the the number of arguments passed
 * @argc: Number of arguments
 * @argv: array of arguments (list)
 * Return: Always 0 (Success)
**/

int main(int argc, char *argv[] __attribute__((unused)))
{
	printf("%d\n", argc);
	return (0);
}

